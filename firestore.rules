rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 공유 DXF 저장소 — 사내 사이트라 별도 인증 없이 read/write 허용.
    // 외부 노출이 우려되면 App Check 또는 익명 인증 + auth != null 으로 강화.
    match /dxf-library/{docId} {
      allow read, write: if true;
    }
  }
}
